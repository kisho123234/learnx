'use client';
import {useState} from 'react';import {api} from '@/lib/api';
export default function AddLesson(){const [form,setForm]=useState({section_id:'1',title:'New Graduation Lesson',type:'video',content_url:'https://www.youtube.com/embed/ysz5S6PUM-U',content:'Lesson description and learning objective.',duration:'08:00'});const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();try{await api(`/sections/${form.section_id}/lessons`,{method:'POST',body:JSON.stringify(form)});setMsg('Lesson added successfully. Open Course 1 to see it.')}catch(e:any){const r=await api('/login',{method:'POST',body:JSON.stringify({email:'teacher@learnx.com',password:'123456'})});localStorage.setItem('learnx_token',r.token);await api(`/sections/${form.section_id}/lessons`,{method:'POST',body:JSON.stringify(form)});setMsg('Logged in as demo teacher and added lesson successfully.')}}return <main className="container section"><span className="tag">Teacher Tool</span><h1 style={{fontSize:44}}>Add Lesson</h1><p className="muted">Add video, PDF, or article lessons to a course section. Section 1 belongs to Course 1 in the demo database.</p><form className="form" onSubmit={submit}><label>Section ID</label><input className="input" value={form.section_id} onChange={e=>setForm({...form,section_id:e.target.value})}/><label>Lesson Title</label><input className="input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><label>Type</label><select className="input" value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option value="video">Video</option><option value="pdf">PDF</option><option value="article">Article</option></select><label>Video/PDF URL</label><input className="input" value={form.content_url} onChange={e=>setForm({...form,content_url:e.target.value})}/><label>Description</label><textarea className="input" rows={4} value={form.content} onChange={e=>setForm({...form,content:e.target.value})}/><label>Duration</label><input className="input" value={form.duration} onChange={e=>setForm({...form,duration:e.target.value})}/><button className="btn" type="submit">Save Lesson</button>{msg&&<p className="muted">{msg}</p>}</form></main>}
